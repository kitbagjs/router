import { Routes } from "./routes";

export type RouteRegex = {name: }
export function routeMatch<T extends Routes>(routes: T): void {

}

// export function match<T extends Routes>(routes: T): T[number] {
//   routes
// }