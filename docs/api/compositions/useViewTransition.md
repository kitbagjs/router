# Compositions: useViewTransition

```ts
const useViewTransition: RouterAssets<RegisteredRouter>["useViewTransition"];
```

A composition to access the view transition in flight, if any.

## Returns

The router's view transition state.
