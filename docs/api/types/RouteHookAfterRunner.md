# Types: RouteHookAfterRunner()

```ts
type RouteHookAfterRunner = (context) => Promise<AfterRouteHookResponse>;
```

## Parameters

| Parameter | Type |
| ------ | ------ |
| `context` | [`AfterHookContext`](AfterHookContext.md) |

## Returns

`Promise`\<[`AfterRouteHookResponse`](AfterRouteHookResponse.md)\>
