# Types: ViewTransitionContext

```ts
type ViewTransitionContext = object;
```

The navigation a view transition animates. There is always a route to animate from, since the first
navigation never transitions.

## Properties

| Property | Type |
| ------ | ------ |
| <a id="from"></a> `from` | [`ResolvedRoute`](ResolvedRoute.md) |
| <a id="to"></a> `to` | [`ResolvedRoute`](ResolvedRoute.md) |
