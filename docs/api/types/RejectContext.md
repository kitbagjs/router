# Types: RejectContext

```ts
type RejectContext = object;
```

The routes a rejection happened between, which rejection hooks are given.

## Properties

| Property | Type |
| ------ | ------ |
| <a id="from"></a> `from?` | [`ResolvedRoute`](ResolvedRoute.md) \| `null` |
| <a id="to"></a> `to?` | [`ResolvedRoute`](ResolvedRoute.md) \| `null` |
